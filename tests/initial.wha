fun fib = %n:i32{
        if n <= 2 {
            return 1;
        };
        return fib(n-1) + fib(n-2);
    } -> i32;

fun main = %{
    i32 i = 1;
    while i <= 10 {
        printint(fib(i));
        i = i + 1;
    };
};
