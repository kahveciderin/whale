fun<void> main = %{
    *char text = "Hello, World!";
    i32 variable = 124;
    print(text);
    text = "np";
    print(text);
    while (variable > 0) {
        printint(variable);
        variable = variable - 1;
    };
};